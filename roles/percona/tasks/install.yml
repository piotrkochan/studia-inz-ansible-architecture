---

- name: "Add Percona yum repository"
  yum:
    name: https://repo.percona.com/yum/percona-release-latest.noarch.rpm
    state: present

- name: "Install percona database server (Percona version < 8)"
  yum:
    name: 
      - "Percona-Server-server-{{ percona__version }}"
      - "Percona-Server-client-{{ percona__version }}"
      - "Percona-Server-devel-{{ percona__version }}"
      - "percona-toolkit"
    state: present

# - name: "Load timezone database into MySQL"
  # shell: "mysql_tzinfo_to_sql /usr/share/zoneinfo | mysql mysql"
